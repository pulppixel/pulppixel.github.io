<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-label">Game Client Engineer / 6+</div>
      <h1>XR부터 멀티플레이어까지,<br>출시로 증명합니다.</h1>
      <p>XR 교육 콘텐츠, 에듀테크, 블록체인 앱, UE5 메타버스, 서바이벌 게임, 실시간 소셜 플랫폼에 이르기까지 다양한 프로젝트의 설계와 구현을 주도하며 모든 프로덕트를 성공적으로 출시/납품했습니다.
        특히 확장성을 고려한 채팅 아키텍처를 단계적으로 고도화해 왔으며, 현재는 Full-stack(Client/Server) 멀티플레이어 환경을 독자적으로 구축하고 있습니다.
        기술 스택 선정부터 코드 문서화까지, 지속 가능한 개발 환경과 유지보수가 용이한 아키텍처를 지향합니다.</p>
      <div class="hero-links">
        <a href="https://www.linkedin.com/in/hwankee-baik-272948266/" target="_blank" rel="noopener">LinkedIn</a>
        <a href="https://github.com/pulppixel" target="_blank" rel="noopener">GitHub</a>
        <a href="mailto:devenvy100@gmail.com">Email</a>
      </div>
    </div>
  </div>
</section>
<style>
  .hero { min-height: 100vh; display: flex; align-items: center; padding-top: 56px; padding-bottom: 15vh; }
  .hero-label { font-family: 'JetBrains Mono', monospace; font-size: 0.75rem; color: var(--accent); letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 1.25rem; opacity: 0; animation: fadeUp 0.6s ease forwards 0.2s; }
  .hero-label::before { content: '> '; color: var(--text-tertiary); }
  .hero .container { margin-left: max(2rem, 10vw); margin-right: auto; }
  .hero h1 { font-family: 'Outfit', 'Noto Sans KR', sans-serif; font-size: clamp(2.5rem, 5vw, 4rem); font-weight: 700; line-height: 1.15; letter-spacing: -0.03em; margin-bottom: 1.5rem; opacity: 0; animation: fadeUp 0.6s ease forwards 0.35s; }
  .hero p { font-size: 1.05rem; color: var(--text-secondary); max-width: 580px; line-height: 1.8; font-weight: 300; opacity: 0; animation: fadeUp 0.6s ease forwards 0.5s; }
  .hero-links { display: flex; gap: 0.75rem; margin-top: 2.5rem; flex-wrap: wrap; opacity: 0; animation: fadeUp 0.6s ease forwards 0.65s; }
  .hero-links a { font-family: 'JetBrains Mono', monospace; font-size: 0.78rem; color: var(--text-secondary); text-decoration: none; padding: 0.5rem 1rem; border: 1px solid var(--border); border-radius: 6px; transition: all 0.2s; }
  .hero-links a:hover { border-color: var(--accent); color: var(--accent); background: var(--accent-dim); }
  .explore-cta { color: var(--accent) !important; border-color: var(--accent-mid) !important; }
  .explore-cta:hover { background: var(--accent-dim) !important; }
  @media (max-width: 480px) { .hero .container { margin-left: 1.25rem; } .hero-links { flex-direction: column; } .hero-links a { text-align: center; } }</style>